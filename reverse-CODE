import time;


def reverse(n):
    m = n;

    while m > 10:
        a = int(m / 10);
        new_num = m - a * 10;
        print(new_num);
        m = int(m / 10);
    print(m);


t1 = time.time();
reverse(9555);
t2 = time.time();
tt = t2 - t1;
print("Total time taken to run this function is:",tt);


SO THATS ALL IN THE CODE... ENJOY...
